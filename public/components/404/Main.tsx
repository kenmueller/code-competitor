import styles from 'styles/components/404/Main.module.scss'

const NotFoundMain = () => (
	<main className={styles.root}>
		<h2 className={styles.title}>Oh no! Are you lost?</h2>
	</main>
)

export default NotFoundMain
